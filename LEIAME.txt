# Instruções do Projeto

# Abra o terminal ou prompt de comando e navegue até a pasta onde você colocou os arquivos do projeto (não use espaço ou acento em nome de pasta)

# Execute o comando abaixo para criar a imagem Docker

docker build -t dm-terraform-image:p .

# Execute o comando abaixo para criar o container Docker

docker run -dit --name dm-p -v D:\PROJETO\Projeto_DM\IaC:/iac dm-terraform-image:p /bin/bash

# Verifique as versões do Terraform e do AWS CLI com os comandos abaixo

terraform version
aws --version

# Configure a chave da AWS com o seguinte comando no container docker: aws configure

Vá para a pasta iac e execute o terraform init